# ===============================
# = DATASOURCE CONFIGURATION =
# ===============================
spring.datasource.url=jdbc:mysql://localhost:3306/order_cart_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=your_password_here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = JPA / HIBERNATE =
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================
# = SERVER CONFIG =
# ===============================
server.port=8080

# ===============================
# = LOGGING =
# ===============================
logging.level.org.springframework=INFO
logging.level.com.programthis=DEBUG

# ===============================
# = ACTUATOR (optional) =
# ===============================
# management.endpoints.web.exposure.include=*

# ===============================
# = CORS (optional) =
# ===============================
# (Puedes configurar CORS globalmente en un config si usas frontend separado)

